// DEPRECATED: replaced by main.navigation.refactored.spec.ts
// Issues: contained a fixed-timeout wait (waitForTimeout), incomplete Docs URL assertion,
// and uses obsolete openDocs/openApi/openCommunity POM methods.

import { test } from "@playwright/test";

test.describe.skip("DEPRECATED – replaced by main.navigation.refactored.spec.ts", () => {
  test("placeholder – do not add tests here", async () => {
    // Intentionally empty.
  });
});
